# 4.1 Deterministic Evaluation: Morning Briefing Refactor

## Verification Objectives
Confirm that the refactored Morning Briefing pipeline executes without hallucination, correctly handles multiple data sources, and performs deterministic calendar writes only after approval.

## Test Matrix

| Test ID | Scenario | Expected Result | Jidoka Gate |
| :--- | :--- | :--- | :--- |
| **MB-001** | Empty Context | Pipeline continues with "No events/tasks found" warning. | Success |
| **MB-002** | Conflicting Slots | `NODE_TIME_BLOCK` identifies conflict and proposes alternative. | Success |
| **MB-003** | Unauthorized write | Pipeline BLOCKED until WhatsApp approval is received. | Success |
| **MB-004** | Direct Tool Fail | `gog` returns 401. Pipeline captures JSON error and halts. | Success |

## The Blindfold Test (Zero Hallucination)
- **Node**: `llm-plan-time-blocks`.
- **Constraint**: The node must output ONLY valid JSON.
- **Validation**: Pass output through `jq .`. If it fails, the node has FAILED the verification gate.

## Physical Verification Protocol
1. **Trigger**: Run `lobster run /home/marku/.openclaw/workspace/Workflows/morning_briefing.lobster`.
2. **Observe**: Monitor WhatsApp for the digest.
3. **Verify**: Use `gog calendar list --today` to confirm that NO events were created before approval.
4. **Approve**: Send "Approve" via WhatsApp.
5. **Final Check**: Use `gog calendar list --today` to confirm the new events exist.
