[package]
name = "collector"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
inotify = "0.10"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schema = { path = "../schema" }

[dev-dependencies]
tempfile = "3"
