# Endpoints

- Optional: transcription services or meeting platforms for read-only retrieval.
- Optional: task or calendar APIs for draft creation if explicitly allowed.
- Do not publish changes without explicit user confirmation.
