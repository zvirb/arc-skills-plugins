# Configuration Context

This folder is for non-secret configuration files, such as:
- Agent behavior settings (`agent.yaml` or `.json`)
- System prompt templates
- Model routing rules

**Rule:** DO NOT put API keys here. Use the `Secrets/` folder instead.
